/** Shared layout + default Mapbox view (Europe, mercator — similar to shuttle booking sites). */
export const MAP_BOOKING_PANEL_HEIGHT_PX = 460;

export const MAP_DEFAULT_CENTER: [number, number] = [10, 48.5];
export const MAP_DEFAULT_ZOOM = 4.2;
